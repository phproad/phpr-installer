<?php

// ------------------------------------------------------------------------
// PHPR bootstrapper
// ------------------------------------------------------------------------

date_default_timezone_set('GMT');
error_reporting(E_ALL);

// ------------------------------------------------------------------------
// Reference to self
// ------------------------------------------------------------------------

$bootstrap_path = __FILE__;

// ------------------------------------------------------------------------
// Application root directory
// ------------------------------------------------------------------------

$application_root =  realpath(dirname(__FILE__));

// ------------------------------------------------------------------------
// Handle resource requests
// ------------------------------------------------------------------------

if (array_key_exists('q', $_GET) && (strpos($_GET['q'], 'javascript_combine/') !== false || strpos($_GET['q'], 'css_combine/') !== false))
{
    include('framework/system/combine_resources.php');
    die();
}

// ------------------------------------------------------------------------
// Load the framework and away we go!
// ------------------------------------------------------------------------

require_once('framework/system/init.php');
